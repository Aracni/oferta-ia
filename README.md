# OFERTA IA

Sistema inteligente de ofertas e afiliados.

## Deploy
Comando de inicialização:
`uvicorn app:app --host 0.0.0.0 --port $PORT`

Variáveis de ambiente:
- SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY

Nunca publique a SERVICE_ROLE_KEY no código.
